let people = require('./people.json')
module.exports = {
  people,

  // Insert values here
  // Access these using the format {{ data['first-name'] or "First name not given" }}
 'first-name': 'Boab',
  'over-18': 'yes'
}
